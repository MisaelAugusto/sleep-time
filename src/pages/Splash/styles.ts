import styled from 'styled-components';

export const Container = styled.div`
  height: 80vh;
  display: flex;
  align-items: center;
  background: #1c1c1c;
  justify-content: center;

  iframe {
    border: 0;
    pointer-events: none;
  }
`;
